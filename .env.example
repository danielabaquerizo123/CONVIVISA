# Configuración del Servidor
PORT=3000
NODE_ENV=development

# Configuración del Frontend
# En producción (Railway), define la URL pública HTTPS del backend.
# Si no se define, el frontend usa rutas relativas /api (proxy de Vite en desarrollo local).
VITE_API_URL="https://backend-ejemplo.up.railway.app"

# Configuración de Base de Datos (PostgreSQL)
# En Railway, esta variable se inyecta automáticamente al conectar el Addon de PostgreSQL.
DATABASE_URL="postgresql://usuario:contraseña@localhost:5432/consvivisa_db?schema=public"

# Seguridad y Autenticación
JWT_SECRET="reemplazar_con_un_secreto_seguro_en_produccion"
JWT_EXPIRATION="24h"

# Configuración fiscal
IVA_RATE=0.15
